# Examples

Examples show how to use RobotDreams with demo models and profiles.

Demo-only robot data can live inside its example folder. Reusable robot data should move to a top-level `models/<robot>/` catalog entry.

# Examples

Examples show how to use RobotDreams with demo models and profiles.

Demo-only robot data can live inside its example folder. Each project folder should prefer a git-friendly `project.json` manifest that references model and artifact files by relative path.

Reusable robot data should move to a top-level `models/<robot>/` catalog entry.

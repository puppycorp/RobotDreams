# Example Assets

Reusable example assets live here so multiple scenes can reference the same robot models, meshes, and model profiles without copying them into each scene directory.

Robot assets should include a `robotdreams.json` model profile next to their exported model files. Scene projects should reference these assets with relative paths from their `project.json`.
